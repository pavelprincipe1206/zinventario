/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Modificado por CARLOS BAZALAR
 * Email: cbazalarlarosa@gmail.com
 * Template pack-angular:src/main/java/dto/EntityDTOService.java.e.vm
 */
package com.incloud.hcp.service.impl;

import com.incloud.hcp.common.enums.AppParametriaLabelEnum;
import com.incloud.hcp.common.enums.AppParametriaModuloEnum;
import com.incloud.hcp.common.excel.ExcelDefault;
import com.incloud.hcp.domain.AppParametria;
import com.incloud.hcp.domain.Boleta;
import com.incloud.hcp.domain.Boleta_;
import com.incloud.hcp.domain.response.BoletaResponse;
import com.incloud.hcp.exception.ServiceException;
import com.incloud.hcp.repository.delta.AppParametriaDeltaRepository;
import com.incloud.hcp.repository.delta.BoletaDeltaRepository;
import com.incloud.hcp.service.BoletaService;
import com.incloud.hcp.service._framework.bean.BeanCargaMasivoDTO;
import com.incloud.hcp.service._framework.bean.BeanListaMasivoDTO;
import com.incloud.hcp.service._framework.impl.JPACustomServiceImpl;
import com.incloud.hcp.service.requireNew.BoletaRequireNewService;
import com.incloud.hcp.service.support.PageRequestByExample;
import com.incloud.hcp.utils.Constants;
import com.incloud.hcp.utils.PredicateUtils;
import org.apache.commons.beanutils.BeanUtils;
import org.apache.commons.lang.StringUtils;
import org.apache.poi.hssf.usermodel.HSSFRow;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.CellStyle;
import org.apache.poi.ss.usermodel.Row;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Example;
import org.springframework.data.domain.ExampleMatcher;
import org.springframework.data.domain.Sort;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;

import javax.persistence.criteria.CriteriaBuilder;
import javax.persistence.criteria.Predicate;
import javax.persistence.criteria.Root;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;

/**
 * A simple DTO Facility for Boleta.
 */
@Service
@Transactional(propagation = Propagation.REQUIRED, rollbackFor = Exception.class)
public abstract class BoletaServiceImpl extends JPACustomServiceImpl<BoletaResponse, Boleta, Integer> implements BoletaService {

    protected final String NAME_SHEET = "Boleta";
    protected final String CONFIG_TITLE = "com/incloud/hcp/excel/BoletaExcel.xml";
    private final Integer REGISTROS_COLOR = 10;

    @Autowired
    protected AppParametriaDeltaRepository appParametriaDeltaRepository;

    @Autowired
    protected BoletaDeltaRepository boletaDeltaRepository;

    @Autowired
    protected BoletaRequireNewService boletaRequireNewService;

    /*****************************/
    /* Metodos de Busqueda       */
    /*****************************/

    protected final ExampleMatcher setAbstractFind(ExampleMatcher matcher) {
        matcher = ExampleMatcher.matching() //
                .withMatcher(Boleta_.direccion.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(Boleta_.nombrePersona.getName(), match -> match.ignoreCase().startsWith())
                .withMatcher(Boleta_.tipoPersona.getName(), match -> match.ignoreCase().startsWith());
        return matcher;
    }

    protected abstract Sort setFindAll(Sort sort);

    protected abstract Sort setFind(Boleta req, ExampleMatcher matcher, Example<Boleta> example, Sort sort);

    protected abstract void setFindPaginated(PageRequestByExample<Boleta> req, ExampleMatcher matcher, Example<Boleta> example);

    protected final Boleta setObtenerBeanResponse(BoletaResponse bean) {
        return bean.getBean();
    }

    protected final Class<Boleta> setObtenerClassBean() {
        return Boleta.class;
    }

    protected List<Predicate> setAbstractPredicate(BoletaResponse bean, CriteriaBuilder cb, Root<Boleta> root) {
        List<Predicate> predicates = new ArrayList<>();
        Boleta entity = bean.getBean();
        /* Obtener condiciones */
        PredicateUtils.addPredicates(predicates, bean.getIdCondicion(), "id", entity.getId(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getDireccionCondicion(), "direccion", entity.getDireccion(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getNombrePersonaCondicion(), "nombrePersona", entity.getNombrePersona(), cb, root);
        PredicateUtils.addPredicates(predicates, bean.getTipoPersonaCondicion(), "tipoPersona", entity.getTipoPersona(), cb, root);
        /* Obtener valores de Lista */
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "id", bean.getIdList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "direccion", bean.getDireccionList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "nombrePersona", bean.getNombrePersonaList(), cb, root);
        PredicateUtils.addPredicatesListValorPrimitivo(predicates, "tipoPersona", bean.getTipoPersonaList(), cb, root);
        return predicates;
    }

    /****************/
    /* METODOS CRUD */
    /****************/

    protected final String validacionesPrevias(Boleta bean) throws Exception {
        String mensaje = "";
        String msg = this.setValidacionesPrevias(bean);
        if (StringUtils.isNotBlank(msg)) {
            mensaje += "* " + msg + "<br/>";
        }
        return mensaje;
    }

    protected String setValidacionesPrevias(Boleta bean) throws Exception {
        String mensaje = "";
        return mensaje;
    }

    protected String validacionesPreviasCreate(Boleta bean) throws Exception {
        String msg = null;
        Boleta validar = null;
        return msg;
    }

    protected String validacionesPreviasSave(Boleta bean) throws Exception {
        String msg = null;
        Boleta validar = null;
        return msg;
    }

    protected Boleta completarDatosBean(Boleta bean) throws Exception {
        BigDecimal data = new BigDecimal(0.00);
        bean = this.setCompletarDatosBean(bean);
        return bean;
    }

    protected Boleta setCompletarDatosBean(Boleta bean) throws Exception {
        return bean;
    }

    protected final Boleta setAbstractCreate(Boleta dto) throws Exception {
        Boleta bean = new Boleta();
        bean = (Boleta) BeanUtils.cloneBean(dto);
        return bean;
    }

    /************************/
    /* METODOS CRUD Masivos */
    /************************/

    protected Boleta setUploadExcel(Cell currentCell, Boleta boleta, int contador) throws Exception {
        Double valorDouble = new Double(0);
        BigDecimal valorDecimal = new BigDecimal(0);
        Boolean valorBoolean = new Boolean(true);
        String valorCadena = "";
        switch (contador) {
        case 1:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 255) {
                    throw new ServiceException("Valor Campo direccion contiene mas de 255 caracter(es)");
                }
                boleta.setDireccion(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo direccion está en formato incorrecto");
            }
            break;
        case 2:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 255) {
                    throw new ServiceException("Valor Campo nombrePersona contiene mas de 255 caracter(es)");
                }
                boleta.setNombrePersona(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo nombrePersona está en formato incorrecto");
            }
            break;
        case 3:
            try {
                valorCadena = currentCell.getStringCellValue();
                if (valorCadena.length() > 2) {
                    throw new ServiceException("Valor Campo tipoPersona contiene mas de 2 caracter(es)");
                }
                boleta.setTipoPersona(valorCadena);
            } catch (Exception e) {
                throw new ServiceException("Valor Campo tipoPersona está en formato incorrecto");
            }
            break;
        default:
            break;
        }
        return boleta;
    }

    protected AppParametria setObtenerRegistroConfiguracionUploadExcel() {
        AppParametria appParametriaData = this.appParametriaDeltaRepository.getByModuloAndLabelAndStatus(AppParametriaModuloEnum.CARGA_EXCEL.getEstado(),
                AppParametriaLabelEnum.INICIO_REGISTRO_DATA.getEstado(), Constants.ESTADO_ACTIVO);
        return appParametriaData;
    }

    protected Boleta setInicializarBeanUpdateExcel() {
        Boleta bean = new Boleta();
        bean.setId(null);
        return bean;
    }

    protected final Integer setObtenerId(Boleta bean) {
        return bean.getId();
    }

    /************************/
    /* Instancia de Bean    */
    /************************/

    protected final Boleta createInstance() {
        Boleta boleta = new Boleta();
        return boleta;
    }

    protected final BeanCargaMasivoDTO<Boleta> createInstanceMasivoDTO() {
        BeanCargaMasivoDTO<Boleta> beanCargaMasivoDTO = new BeanCargaMasivoDTO<Boleta>();
        return beanCargaMasivoDTO;

    }

    protected final BeanListaMasivoDTO<BeanCargaMasivoDTO<Boleta>> createInstanceListaMasivoDTO() {
        BeanListaMasivoDTO<BeanCargaMasivoDTO<Boleta>> beanCargaMasivoDTOBeanListaMasivoDTO = new BeanListaMasivoDTO<BeanCargaMasivoDTO<Boleta>>();
        return beanCargaMasivoDTOBeanListaMasivoDTO;
    }

    /*****************************/
    /* Metodos que generan Excel */
    /*****************************/

    protected String devuelveNombreSheet() {
        return this.NAME_SHEET;
    }

    protected String devuelveListaHeaderExcelXML() {
        return this.CONFIG_TITLE;
    }

    protected int setAbstractDownloadExcel(Boleta bean, HSSFRow dataRow) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getDireccion(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getNombrePersona(), dataRow.createCell(contador));
        contador++;
        ExcelDefault.setValueCell(bean.getTipoPersona(), dataRow.createCell(contador));
        contador++;
        return contador;
    }

    protected int setAbstractDownloadExcelSXLSX(Boleta bean, Row dataRow, List<CellStyle> cellStyleList) {
        int contador = 0;
        ExcelDefault.setValueCell(bean.getId(), dataRow.createCell(contador), "I", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getDireccion(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getNombrePersona(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        ExcelDefault.setValueCell(bean.getTipoPersona(), dataRow.createCell(contador), "S", cellStyleList);
        contador++;
        return contador;
    }

    protected String setAbstractGenerateInsertExcelSXLSX(Boleta bean) {
        String fechaS = "";
        String sqlInsert = "INSERT INTO BOLETA(";
        sqlInsert = sqlInsert + "ID_BOLETA" + ", ";
        sqlInsert = sqlInsert + "DIRECCION" + ", ";
        sqlInsert = sqlInsert + "NOMBRE_PERSONA" + ", ";
        sqlInsert = sqlInsert + "TIPO_PERSONA" + ")";
        sqlInsert = sqlInsert + " VALUES (";
        sqlInsert = sqlInsert + bean.getId() + ", ";
        if (StringUtils.isBlank(bean.getDireccion())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getDireccion() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getNombrePersona())) {
            sqlInsert = sqlInsert + "null" + ", ";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getNombrePersona() + "'" + ", ";
        }
        if (StringUtils.isBlank(bean.getTipoPersona())) {
            sqlInsert = sqlInsert + "null";
        } else {
            sqlInsert = sqlInsert + "'" + bean.getTipoPersona() + "'";
        }
        sqlInsert = sqlInsert + " );";
        return sqlInsert;
    }

    /*****************/
    /* Otros Metodos */
    /*****************/

}
